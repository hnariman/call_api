# a simple async request to test API with Rust

This is a part of series for cli & tui (ncurses) todo application, with ability
to sync with Google Tasks & Google Calendar, also potentially with integration
with Calendly
